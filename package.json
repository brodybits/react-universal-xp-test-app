{
  "name": "react-universal-test-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "echo 'not supported' && exit 1",
    "start-web": "react-scripts start",
    "build": "echo 'not supported' && exit 1",
    "build-web": "react-scripts build",
    "test": "echo 'not supported' && exit 1",
    "eject": "echo 'not supported' && exit 1",
    "eject-web": "react-scripts eject"
  },
  "dependencies": {
    "react": "16.5.1",
    "react-dom": "^16.8.6",
    "react-native-web": "^0.11.2",
    "react-primitives": "^0.8.0",
    "react-scripts": "3.0.0"
  },
  "devDependencies": {
    "babel-preset-expo": "^5.1.1"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
